To create the images that LaTeX can use:
- export to SVG (the "Scalable Vector Graphics" one, as there are many)
- load that .svg into gimp, and use a scaling ration of 2.0 (so the image will be about 2000-3000 pixels wide)
- change to grayscale
- save as a .png
